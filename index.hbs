{{!< default}}
{{! The tag above means - insert everything in this file into the {body} of the default.hbs template }}

{{! The big featured header }}
<header id="header" class="{{#if @blog.cover}}">
    <h1><a href="{{@blog.url}}">{{@blog.title}}</a></h1>
    {{navigation}}
</header>

<!-- Banner -->
<section id="banner" style="background-image: url({{@blog.cover}}){{else}}no-cover{{/if}}">
    <h2>{{@blog.description}}</h2>
</section>

<!-- One -->
<section id="one" class="wrapper style1 align-center">
    <div class="container">
        <header>
            <h2>New - Fresh - jQuery free</h2>
            <p>jHERE 1.0 beta is out, give it a spin.</p>
        </header>
        <div class="row 200%">
            <section class="4u 12u$(small)">
                <i class="icon big rounded fa-thumbs-o-up"></i>
                <p>EASY</p>
                <p>With jHERE you can make maps with just one line of code.</p>
            </section>
            <section class="4u 12u$(small)">
                <i class="icon big rounded fa-rocket"></i>
                <p>FAST</p>
                <p>jHERE 1.0 doesn't need any framework, and it automatically adds to the page what is needed from HERE Maps API.</p>
            </section>
            <section class="4u$ 12u$(small)">
                <i class="icon big rounded fa-github-alt"></i>
                <p>FREE AND OPEN SOURCE</p>
                <p>Take a look at the source code, improve it and contribute back if you'd like.</p>
            </section>
        </div>
    </div>
</section>

{{! The main content area on the homepage }}
<section id="two" class="wrapper style2 align-center">
    <div class="container">
        <header>
            <h2>Built with jHERE</h2>
            <p>Examples of what you can make with jHERE</p>
        </header>
        <div class="row">
            {{! The tag below includes the post loop - partials/loop.hbs }}
            {{> "loop"}}
        </div>
    </div>
</section>
